/*
Package jolokia implements the core domain logic for Jolokia sidecar injection.

It provides annotation parsing, validation, and Pod mutation for injecting the
Jolokia JVM agent as a native sidecar (KEP-753) init container.
*/
package jolokia
